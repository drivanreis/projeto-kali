[DIRETIVA DE EXECUÇÃO - FASE 3: INJEÇÃO LÓGICA DO ALVO DINÂMICO]

Cascade, com base na estrutura que você acabou de mapear no `frontend/index.html`
 (onde identificou o `target-input` e o grid de 3 colunas da `tela-arsenal`),
  precisamos garantir que as duas telas funcionem em sincronia total.

Abra o arquivo javascript responsável pelo controle do painel (geralmente dentro
 de `frontend/js/` ou conforme indicado no README_WEB_DASHBOARD.md) e verifique
  a função `iniciarOperacao()`.

Sem quebrar o layout atual, adicione um validador simples para que,
 se o `target-input` estiver vazio, o sistema exiba um alerta visual
 sutil na tela (em vez de um alert do navegador tradicional) antes de
 disparar o log stream do websocket. Mostre apenas a alteração cirúrgica
 aplicada no script correspondente.
